;; leading comment on the file
(define (sum-values values) ; trailing comment on the head line
;; own line comment inside the body
(fold + 0 values))

(define layout #|inline block|# (make-layout 3 4))

(list 1 ; one
2 ; two
3)
